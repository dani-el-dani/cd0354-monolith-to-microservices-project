apiVersion: v1
kind: Service
metadata:
  labels:
    run: backend-feed
  name: backend-feed
  namespace: default
  selfLink: /api/v1/namespaces/default/services/backend-feed
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: backend-feed
